<Project>
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>13.0</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <EnableNETAnalyzers>true</EnableNETAnalyzers>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <Deterministic>true</Deterministic>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>

    <!-- Package/assembly metadata. The hosted layer consumes this engine as a
         NuGet package, so every packable project carries the OSS license. -->
    <Authors>Waleed Zafar</Authors>
    <Company>EMaigrator</Company>
    <Copyright>Copyright 2026 Waleed Zafar</Copyright>
    <PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/waleedzafar68/EMaigrator</PackageProjectUrl>
  </PropertyGroup>

  <!-- Test projects use xUnit's Method_Scenario_Expectation naming; CA1707 (no underscores
       in member names) is noise there. CA1861 (prefer static readonly over constant array
       arguments) is a micro-perf rule that is noise for inline assertion arrays like
       Should().Equal(new[] { ... }). Both are scoped to *.Tests / *.IntegrationTests by name;
       production code still enforces them. -->
  <PropertyGroup Condition="$(MSBuildProjectName.EndsWith('Tests'))">
    <NoWarn>$(NoWarn);CA1707;CA1861</NoWarn>
  </PropertyGroup>
</Project>
